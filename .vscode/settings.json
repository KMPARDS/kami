{
  "typescript.tsdk": "./node_modules/typescript/lib", // we want to use the TS server from our node_modules folder to control its version
  "editor.formatOnSave": true, // formating on save
  "deno.enable": false // disable deno vscode plugin if it is enabled since this is node js project
}
